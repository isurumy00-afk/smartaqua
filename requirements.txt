# Smart Aquarium Monitoring System — Raspberry Pi 4B Dependencies

# Core numeric / data
numpy>=1.24.0
pandas>=2.0.0

# Computer vision (headless, no GUI)
opencv-python-headless>=4.8.0

# ONNX Runtime — unified CPU inference for YOLOv8 + disease classifier
# Works on both aarch64 (Pi 4B) and x86-64 dev machines
onnxruntime>=1.16.0

# Water quality ML (scikit-learn / joblib only — no ONNX conversion needed)
scikit-learn>=1.3.0
joblib>=1.3.0

# Explainability
shap>=0.42.0
matplotlib>=3.7.0

# IoT / hardware interfaces (Debian 13 Trixie / Python 3.13 lgpio support)
pyserial>=3.5
pymodbus>=3.0.0
rpi-lgpio>=0.6; platform_system == 'Linux'
gpiozero>=2.0; platform_system == 'Linux'

# Firebase cloud upload
firebase-admin>=6.2.0

# Web dashboard
Flask>=3.0.0
